class Solution:
    def minElement(self, nums):
        ans = float('inf')

        for num in nums:
            s = 0
            while num > 0:
                s += num % 10
                num //= 10
            ans = min(ans, s)

        return ans
